// Placeholder — the real Drizzle schema (Initiative, IntakeVersion,
// RiskAssessment, ReviewCycle, ReviewDecision, DeploymentVersion,
// ControlDefinition, EffectiveControl, Observation, Incident, AuditEvent,
// RunBudget) is defined per plan.md §5, including the uniqueness/linkage
// constraints and the DB-level append-only enforcement on AuditEvent.
// Intentionally left unimplemented (P1 task) — do not add tables here yet.
export {};
